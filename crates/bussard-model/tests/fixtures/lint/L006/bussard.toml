# Fixture for lint L006: the light block's feedback slot 1/1/3 is empty.

[connection]
transport = "tunnel"
gateway = "127.0.0.1:3671"

[lint.groups]
scheme = "floor-trade-block"
feedback_pairing = true

[lint.groups.blocks]
light = 5
